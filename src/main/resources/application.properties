spring.application.name=orange

#전체 로그 레벨 설정(기본 info)
logging.level.root=info

#fruit.orage 패키지와 그 하위 로그 레벨 설정
#logging.level.fruit.orange=info

#라이브로 바로 반영(실시간)
spring.devtools.livereload.enabled=true

#[db] postgreSQL 
spring.datasource.hikari.maximum-pool-size=4
spring.datasource.url=jdbc:postgresql://localhost:5432/orange
spring.datasource.username=postgres
spring.datasource.password=orange123
spring.datasource.driver-class-name=org.postgresql.Driver

# mybatis
mybatis.config-location=classpath:mybatis/configuration.xml
#mybatis.config=mybatis/mybatis-config.xml
#mybatis.type-aliases-package=com.first
#mybatis.mapper-locations=classpath:mybatis/mapper/**/*.xml
#mybatis.configuration.map-underscore-to-camel-case=true

#[test] static file path settings
spring.mvc.static-path-pattern=/static/**